"""Intent classifier training package.

This package is
training-only — it must NOT be imported from hub/edge runtime code.  Edge
inference uses hub/edge/agent/intent_classifier.py (ONNX runtime only).
"""
